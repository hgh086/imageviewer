# imageviewer
image viewer using raylib

use c,janet,tjs,bun,deno,vox,hook,scopes,ring,terra(luajit),odin,factor,raku(perl6),dascript(daslang),racket,zig ... language to implement it.

I wrote the zig imageviewer code by the help with ai.
